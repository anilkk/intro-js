const name = 'Naveen';
document.querySelector('#name').innerText = name;

// Add number 1 to 10
for (var i = 1; i < 11; i++) {
    document.querySelector('.numbers').innerHTML += "<div>" + i + "</div>";
}